@page
@model ErrorModel
@{
    ViewData["Title"] = "Hata";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <!-- Hata Başlığı -->
            <div class="alert alert-danger text-center shadow-lg">
                <h1 class="display-3 text-danger"><span role="img" aria-label="exclamation mark">❌</span> <strong>Hata</strong> <span role="img" aria-label="exclamation mark">❌</span></h1>
                <h2 class="h4 text-muted">Bir şeyler yanlış gitti! İsteğiniz işlenirken bir hata oluştu. <span role="img" aria-label="thinking face">🤔</span></h2>
            </div>

            <!-- Request ID -->
            @if (Model.ShowRequestId)
            {
                <div class="alert alert-info shadow-sm">
                    <p><strong>İstek ID:</strong> <code>@Model.RequestId</code> <span role="img" aria-label="memo">📝</span></p>
                </div>
            }

            <!-- Geliştirme Modu Uyarısı -->
            <div class="card mt-4 border-info shadow-sm">
                <div class="card-header bg-warning text-white">
                    <h3 class="card-title"><span role="img" aria-label="wrench">🔧</span> Geliştirme Modu</h3>
                </div>
                <div class="card-body">
                    <p class="card-text">Geliştirme ortamında, oluşan hata hakkında daha detaylı bilgi görüntülenebilir. <span role="img" aria-label="eyes">👀</span></p>
                    <p><strong>Uyarı:</strong> <strong>Geliştirme</strong> ortamı üretim ortamında etkin olmamalıdır! Bu, kullanıcıların hassas hata bilgilerine erişmesine neden olabilir. <span role="img" aria-label="warning">⚠️</span></p>
                    <p>Yerel hata ayıklama için, <strong>Geliştirme</strong> ortamını etkinleştirebilir ve ardından uygulamayı yeniden başlatabilirsiniz. Bunun için <strong>ASPNETCORE_ENVIRONMENT</strong> ortam değişkenini <strong>Development</strong> olarak ayarlayın. <span role="img" aria-label="gear">⚙️</span></p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap ve Google Fontlar için Bağlantılar -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEJ0RZt8k8k4t+G6yYYX2ShD24tZzMJ6Ed59wz5no/BB93XJbqbgEKk5ceBeF" crossorigin="anonymous">
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
